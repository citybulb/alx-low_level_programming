#include "main.h"

/**
 * _puts_recursion - prints a string followed by \n
 * @s: string to output
 * Return: Nothing
 */

void _puts_recursion(char *s)
{

  11;rgb:0000/0000/0000if (*s != '\0')
{
_putchar(*s);
s++;
}

else
{
return;
}

_puts_recursion(s);

}
